<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <Description>Orleans hosting for Orleans.Dataflow: coordinator and run grains, the silo registration surface, and the remote run handle.</Description>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.PublicApiAnalyzers">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.Orleans.Sdk" />
    <PackageReference Include="Microsoft.Orleans.Server" />
    <!--
      Server is Runtime plus Persistence.Memory plus Sdk plus Core and carries neither reminders nor
      broadcast channels, so the two trigger-and-bridge adapters name their own packages: Reminders for
      IRemindable and the registry behind it, BroadcastChannel for the channel writer the sink publishes
      through.
    -->
    <PackageReference Include="Microsoft.Orleans.BroadcastChannel" />
    <PackageReference Include="Microsoft.Orleans.Reminders" />
    <!--
      The stream adapters need the provider abstractions and the stream implementation types; Server is
      Runtime plus Persistence.Memory plus Sdk plus Core and deliberately carries no Streaming.
    -->
    <PackageReference Include="Microsoft.Orleans.Streaming" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="../Orleans.Dataflow/Orleans.Dataflow.csproj" />
  </ItemGroup>
</Project>
